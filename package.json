{
  "name": "script2video",
  "version": "1.0.0",
  "description": "Paste a script, pick a caption style, get one MP4 with a voiceover and burned-in subtitles, plus scene suggestions.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.js",
    "doctor": "node scripts/doctor.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "express": "^5.2.1",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
